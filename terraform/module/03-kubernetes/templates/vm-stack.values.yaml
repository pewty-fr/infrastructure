vmsingle:
  enabled: false

vmcluster:
  enabled: true
  spec:
    retentionPeriod: "365"
    vmstorage:
      storage:
        volumeClaimTemplate:
          spec:
            storageClassName: "scw-bssd"
            resources: 
              requests:
                storage: "20Gi"
    vmselect:
      storage:
        volumeClaimTemplate:
          spec:
            storageClassName: "scw-bssd"
            resources: 
              requests:
                storage: "2Gi"

alertmanager:
  spec:
    externalURL: "https://${ALERTMANAGER_DOMAIN}"
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: "scw-bssd"
          resources: 
            requests:
              storage: "2Gi"
  ingress:
    enabled: true
    ingressClassName: "traefik"
    hosts:
    - ${ALERTMANAGER_DOMAIN}

grafana:
  persistence:
    enabled: true
    storageClassName: "scw-bssd"
  ingress:
    enabled: true
    ingressClassName: "traefik"
    hosts:
    - ${GRAFANA_DOMAIN}

kubeEtcd:
  enabled: false
